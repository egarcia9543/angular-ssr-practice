/**
 * Rutas de la aplicación.
 *
 * Los títulos y descripciones que antes vivían aquí se movieron a
 * `src/app/config/seo.config.ts`: un título de página es un dato de SEO, no
 * una constante de enrutamiento, y mantenerlos juntos obligaba a tocar dos
 * conceptos distintos en el mismo archivo.
 */
export enum AppUrls {
  ABOUT = '/about',
  CONTACT = '/contact',
  PRICING = '/pricing',
  CHARACTERS = '/characters/page/1',
}
